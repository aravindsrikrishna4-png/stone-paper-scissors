#include <stdio.h>
int main()
{
    float a,b;
    printf("Enter the value of the first number:");
    scanf("%d",&a);
  printf("Enter the value of the second number:");
    scanf("%d",&b);
    float c;
    c=a*b;
    printf("The product of the first and second number is: %f",c);
    return 0;
    
}